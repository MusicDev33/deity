/*
The engine for the simulation. The loop will be in the index file, but the updates and stuff like that are found here.
*/

const delay = (ms: number) => new Promise(res => setTimeout(res, ms));

export const update = async () => {
  console.log('test');

}